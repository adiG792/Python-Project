import imp_file

print(imp_file.roll_dice(10))
# we have thousands of external modules in lib/ in python source code
# we can also install third party files


# installing third party modules
# by installing pip(aka package manager)
# install by command prompt
# check if python is installed or not, ( pip --version )
# pip install (python module you want to install without bracket)
# module is installed in /lib/site-packages
# to uninstall
# pip uninstall (python module you want to uninstall without bracket)
